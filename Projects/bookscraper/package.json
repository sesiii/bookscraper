{
    "name": "scrapy-runner",
    "version": "1.0.0",
    "description": "A Node.js project for running Scrapy",
    "scripts": {
      "prestart": "python3 -m venv env && source env/bin/activate && pip install scrapy",
      "start": "source env/bin/activate && python run_scrapy.py"
    }
  }